const numbersArray = [9, 2, 4, 5, 8, 5, 6, 1, 1, 0];
console.log(`sorted numbers in ascending order are: ${numbersArray.sort((a, b) => a - b)}`);
console.log(`sorted numbers in descending order are: ${numbersArray.sort((a, b) => b - a)}`);
